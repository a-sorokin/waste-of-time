export enum EXPRESSION_TYPES {
  literal = 'literal',
  function = 'function',
}

export enum EXPRESSION_NAMES {
  add = 'add',
  equals = 'equals',
  not = 'not',
  contains = 'contains',
  fetchGet = 'fetchGet',
}
